node {
    stage 'Ask Parameter'
            input message: 'What\'s the candidate id?', parameters: [string(defaultValue: '', description: '', name: 'CANDIDATE')]
            echo '${params.CANDIDATE}'
}